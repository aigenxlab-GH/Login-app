server:
  # Local-dev port — matches .env (PORT=8085) and docs.
  # Honors $PORT env var if set; defaults to 8085.
  port: ${PORT:8085}

logging:
  level:
    com.aigenxlab.loginapp: DEBUG
    org.springframework.security: DEBUG
